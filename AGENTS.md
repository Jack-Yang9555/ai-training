# 智谱中联平台 BUG 及功能修复——会话入口

> 适用范围：本仓库及全部子目录。

## 1. 项目入口

本仓库当前的主要应用位于 `demo/`，项目名称为“启境 AI Learning OS Demo”。

开始任何项目工作前，必须按以下顺序恢复上下文：

1. 完整阅读 `demo/AGENTS.md`，了解产品目标、角色边界、技术约束与验收基线。
2. 阅读 `demo/README.md`，了解启动方式和推荐演示路径。
3. 阅读 `demo/PROJECT_STATUS.md`，了解最近进度、验证结果、已知问题和下一步。
4. 运行 `git status --short`，确认用户已有修改，不得覆盖或清理无关内容。
5. 检查与当前任务直接相关的实现，不仅依赖进度摘要作出判断。

## 2. 进度交接规则

每次完成实质性开发、修复、诊断或验收后，在最终交付前必须更新 `demo/PROJECT_STATUS.md`。

更新时应：

- 使用具体日期和简短的任务摘要。
- 记录实际完成内容，不把需求基线当作已完成功能。
- 记录已运行的验证命令及结果；未验证的项目明确标记“未运行”。
- 记录遗留问题、阻塞项和可执行的下一步。
- 保留与当前状态有关的最近交接记录，删除已被后续结论完全替代的过时描述。
- 如果本次只是普通问答，未得出新的项目结论且未改变仓库，可不更新进度文件。

## 3. 实现与验收

- 所有 `demo/` 内的产品、视觉、角色权限和工程规则以 `demo/AGENTS.md` 为准。
- 修改前先检查现有实现，避免重复组件、重复页面或第二套视觉系统。
- 根据改动风险运行类型检查、测试、构建和真实浏览器验证。
- 进度文件是交接摘要，不替代代码、Git 差异、测试结果或需求文档。
